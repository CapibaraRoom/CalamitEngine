#!/bin/sh

cd
curl -fsSL https://xmake.io/shget.text | bash
cd
mkdir -p xmake/usr/local/bin
mkdir -p xmake/usr/local/share/xmake
cp -r .local/bin/* xmake/usr/local/bin/
cp -r .local/share/xmake/* xmake/usr/local/share/xmake/
